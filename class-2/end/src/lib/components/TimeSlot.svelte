<script>
	export let timeslotUrl;

	/**
	 * Assignment: instead of showing the URL; we want to show the date and time of the timeslot
	 * Please make sure to add an async function that fetches the date and time of the timeslot
	 * and add it to the DOM.
	 */
</script>

<div>
	<p>{timeslotUrl}</p>
</div>
